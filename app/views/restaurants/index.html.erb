<%= render "navbar" %>

<h1>Restaurants</h1>
<ul>
  <% @restaurants.each do |restaurant| %>
    <li><%= link_to restaurant.name, restaurant_path(restaurant) %> - category: <%= restaurant.category %> - <%= restaurant.address %> - <%= restaurant.phone_number %></li>
  <% end %>
</ul>
